namespace Shared.Enums
{
    // Type de vérification pour l'authentification
    public enum VerificationType
    {
        // Vérification par email
        Email = 1,

        // Vérification par téléphone
        Telephone = 2
    }
}